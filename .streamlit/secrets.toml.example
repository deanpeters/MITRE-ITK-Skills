# Local development only. Copy to .streamlit/secrets.toml and add your key.
# secrets.toml is gitignored and never committed.
#
# On Streamlit Community Cloud, set ANTHROPIC_API_KEY in the app's
# Secrets settings — users of the deployed app never need their own key.

ANTHROPIC_API_KEY = "sk-ant-..."
